using DownloadFile.Models;
using Microsoft.AspNetCore.Mvc;
using System.Diagnostics;

namespace DownloadFile.Controllers
{
    public class HomeController : Controller
    {
        /*public void CallTheFunction(string name, int numTime=1)
        {
            HelloWorldController hello=new HelloWorldController();
            hello.Index();
            hello.Welcome(name,numTime);
        }*/
    }
}