<UserControl x:Class="ContactApp.Controllers.ContactControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ContactApp.Controllers"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="800">
    <Grid Background="#AAE5E5E5">
        <!--<ListView  Margin="5,6,5,5" Background="Honeydew">
            <ListView.View>
                <GridView x:Name="ContactDataView">
                    --><!-- <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}" Width="50"/>
                    <GridViewColumn  Header="{Binding}" DisplayMemberBinding="{Binding Name}" Width="250"/>
                    <GridViewColumn  Header="{Binding}" DisplayMemberBinding="{Binding Email}" Width="300"/>--><!--
                    <GridViewColumn  Header="{Binding}" DisplayMemberBinding="{Binding PhoneNumber}" Width="250"/>
                </GridView>
            </ListView.View>
        </ListView>-->
    </Grid>
</UserControl>
